/*package Productos;

import javax.swing.*;

public class ProdConsExample {
    private static ListaEnteros listaEnteros = new ListaEnteros(); //ME DA ERROR EN LISTAENTEROS, SI POR QUE FALTA LO OTRO PS
    private static ListaEnteros monitor = new ListaEnteros();

    public static ListaEnteros getListaEnteros() {
        return listaEnteros;
    }

    public static ListaEnteros getMonitor() {
        return monitor;
    }

    public static void mostrarListaEnteros() {
        if (listaEnteros.tam == 0) {
            System.out.println("La lista enteros esta vacia[]");
            return;
        }

        System.out.println("Contenido del array");

        int tam = listaEnteros.tam;
        int[] arrayEnteros = listaEnteros.getArray();
        for (int i = 0; i < tam; i++) { 
            System.out.print("[" + arrayEnteros[i] + "]");
        }
        System.out.println("");
    }
}*/
